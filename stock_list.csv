import akshare as ak
import pandas as pd

df = ak.stock_zh_a_spot_em()
df = df[df['代码'].str.match(r'(60|00|30)')]
df[['代码', '名称']].to_csv('stock_list.csv', index=False, encoding='utf-8')
print(f"已生成 {len(df)} 只股票的列表，文件名为 stock_list.csv")
